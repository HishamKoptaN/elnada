name: CI/CD Pipeline (Powered by DevOps Core) #1

on:
    push:
        branches: [dev, prod]
    workflow_dispatch:

jobs:
    workflows:
        uses: HishamKoptaN/flutter-devops-core/.github/workflows/main.yaml@prod
        secrets: inherit
